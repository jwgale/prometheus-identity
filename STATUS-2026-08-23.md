# Prometheus overnight status

Date: 23 August 2026.

This page is for Jason Gale. This package is laboratory code under PolicyLab-2. This package is not Sanctum.

This document uses ASD-STE100 Simplified Technical English. Technical names appear in full words.

## Closed

VISION rungs 51 through 105 are closed. The laboratory test count is 522 tests.

Rungs 51 through 64 closed the durable agent process, public-check fail-closed leftovers, well-known honesty, WIMSE and multi-act walks, add-act, named act, one-shot mix refuse, and runtime parse honesty.

Rungs 65 through 91 closed the later user interface typed check base, Check again, Check both, named act, and the live public proofs of those verbs against https://check.prestigeworldwide.digital. GET /laboratory Check again is closed. GET /laboratory does not claim Check both or a named act.

Rung 92 closed the challenge-after-remaining-life hole. Kernel issue_holder_challenge and POST /challenge append a signed issuance.log line. After remaining life that write is refused. POST /verifier-challenge and GET /instances stay allowed.

Rung 93 closed the member-two and threshold leftover after the seal write. Kernel add_issuer_member, set_issuer_threshold, and set_verify_threshold wrote issuer.json during remaining life. The host already refused those paths. The command line can skip the host. The kernel now refuses the same way as rotate.

Rung 94 closed the intended-export over-refuse after remaining life. sign_issuance_log_tree_head refused after remaining life and blocked seal-export and kill-export. Those exports must still produce a document a verifier can accept. Kill after remaining life was already allowed. Mint, birth, spawn, present, check, challenge, rotate, member-two, and both thresholds stay refused.

Rung 95 locked the remaining verifier artifacts after remaining life. Act-export of a historical receipt, previous-key-export, and GET /issuer-public were already allowed. Named allow tests now prove those paths. A sealed issuing store still cannot pin a foreign issuer public key. Issuing-host start after remaining life stays refused. That is a start gate, not a per-path over-refuse.

Hunt after Rung 95 (Rung 96 candidate, proof only, 23 August 2026). prepare_host_store (host.rs:2705) calls require_issuer_not_sealed when the host is not check-only. require_issuer_not_sealed refuses when is_sealed_at(now) is true. CLI `prometheus host` calls run_host_from_flags, then run_host_with_mode, then prepare_host_store. A fresh issuing-host start after remaining life refuses. Existing test check_only_host_starts_without_a_live_mint_issuer_and_stays_loopback (host.rs:5642) already expects that refuse, and allows check-only start. SOURCE-OF-TRUTH.md and ANATOMY.md name GET / as the later user interface. They name export, kill, and listing after remaining life as path allows. They do not name issuing-host start after remaining life as a required allow or a required refuse. JUDGE-NON-CLI.md says host start is a listen command so the page exists. That page does not require start after remaining life. A host that is already running can export after remaining life (Rung 94 and Rung 95). Command-line export after remaining life already works. This hunt does not unlock start. This hunt does not invent a product lock. Rust was not changed. The laboratory test count stays 520 tests.

Rung 96 closed member two remote path over site-to-site. A throwaway issuing store wrote member two through an SSHFS mount of prometheus-member-two. Birth without that path refused. Birth with --member-secret on the mount succeeded. Public check was not done. Rust was not changed. The laboratory test count stays 520 tests.

Rung 97 closed the public allow-then-refuse on that remote path. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. POST /check-svid allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not used. Rust was not changed. The laboratory test count stays 520 tests.

Rung 98 closed later user interface Create Agent Principal on that remote path. GET / on 127.0.0.1:18832 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth with the path after fusermount refused. Birth and present-svid with the remounted path succeeded with two member signatures. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-member-two-remote.

Rung 99 closed later user interface Create Agent Principal and Assertion Act on that remote path, then public allow then refuse. GET / on 127.0.0.1:18834 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-svid with the remote path succeeded with two member signatures. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-svid allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-member-two-public.

Rung 100 closed later user interface Create Agent Principal and WIMSE Assertion Act on that remote path, then public allow then refuse. GET / on 127.0.0.1:18836 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-wimse with the remote path succeeded with two member signatures. The Workload Identity Token subject is the present-hash wimse Uniform Resource Identifier. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-wimse allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-member-two-public-wimse.

Rung 101 closed laboratory operator page Create Agent Principal on that remote path. GET /laboratory on 127.0.0.1:18838 is the laboratory operator page. GET / on the same host is the later user interface and is a different page. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth with the path after fusermount refused. Birth and present-svid with the remounted path succeeded with two member signatures. Public check of this present was not done. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-laboratory-member-two-remote.

Rung 102 closed laboratory operator page Create Agent Principal and Assertion Act on that remote path, then public allow then refuse. GET /laboratory on 127.0.0.1:18842 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-svid with the remote path succeeded with two member signatures. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-svid allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-laboratory-member-two-public.

Rung 103 closed laboratory operator page Create Agent Principal and WIMSE Assertion Act on that remote path, then public allow then refuse. GET /laboratory on 127.0.0.1:18844 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-wimse with the remote path succeeded with two member signatures. The Workload Identity Token subject is the present-hash wimse Uniform Resource Identifier. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-wimse allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-laboratory-member-two-public-wimse.


Rung 104 closed `prometheus runtime-check agent-process` on hostname 5090 with the remote member-secret path, then public allow then refuse. Create Agent Principal stayed on 127.0.0.1:18846. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-svid with the remote path succeeded with two member signatures. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. LaboratoryRuntime still refused --holder-secret-path. Agent-process stayed up as process identifier 3559917. The first tool line printed ALLOWED and ran the tool. After Decommission, kill export, and public kill-accept, a second tool line on that same process identifier printed REFUSED because this store accepted a kill. The tool did not run. The process did not restart. Hermes was not started. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/member-two-agent-process.

Rung 105 closed the issuer-threshold raise leftover. Kernel set_issuer_threshold saved issuer.json at the new n, then signed the issuance.log line at that new n. When the outside member secret was not presented, the sign refused after n=2 was already persisted. The command line can skip the host. The kernel now checks member secrets for the new n before any write. A refused raise leaves threshold_n at 1. Same-n after n=2 stays a no-op. The laboratory test count is 522 tests.

The public check name is check.prestigeworldwide.digital. That host is check only. Create Agent Principal stays on 127.0.0.1.

See JUDGE-NON-CLI.md for what a person can prove without the command line. Init stays on the command line. Host start is a listen command.

This page does not say the bet is won.

GitHub publication started and landed on 23 August 2026. The public repository is https://github.com/jwgale/prometheus-identity. The repository name is the Cargo package name prometheus_identity. That name is not a market name. issuer.secret, biscuit secrets, holder secrets, member-two secrets, data directories, and prometheus-lab-vpc were not published. GitHub Actions were not added.

## Parked

SPIRE is parked. SPIRE starts only as a consumer when a runtime will not call loopback /check-svid or https://check.prestigeworldwide.digital. Do not start SPIRE.

Do not invent more presenter-pair walks. Do not polish Birth, Present, and Death labels on GET /laboratory.

## Next unfinished

The issuer-threshold raise without the outside member secret is closed as Rung 105. Remaining leftovers are parked or are not coding slices: n=3, SPIRE, Check-both matrix, issuing-host start after remaining life, /laboratory label polish, stolen-issuer close, authorization talk, market holder, standing data-a raise, how a later market names the well-known check, and a later full header-coverage HTTP Message Signatures stack. That later stack is not a startable fail-closed leftover without opening more headers than the locked method, request-target, and content-digest bind. Do not invent a market holder for member two. Do not start SPIRE. Do not raise standing data-a. Do not unlock issuing-host start after remaining life. Do not invent another operator-page pairing.

VISION-open items that are not coding slices: who holds member two in a later market, a later market name for the well-known check, and the stolen-issuer limit while a verifier has not accepted seal.

# Prometheus overnight status

Date: 23 August 2026.

This page is for Jason Gale. This package is laboratory code under PolicyLab-2. This package is not Sanctum.

This document uses ASD-STE100 Simplified Technical English. Technical names appear in full words.

## Closed

VISION rungs 51 through 106 are closed. The laboratory test count is 523 tests.

Rungs 51 through 64 closed the durable agent process, public-check fail-closed leftovers, well-known honesty, WIMSE and multi-act walks, add-act, named act, one-shot mix refuse, and runtime parse honesty.

Rungs 65 through 91 closed the later user interface typed check base, Check again, Check both, named act, and the live public proofs of those verbs against https://check.prestigeworldwide.digital. GET /laboratory Check again is closed. GET /laboratory does not claim Check both or a named act.

Rung 92 closed the challenge-after-remaining-life hole. Kernel issue_holder_challenge and POST /challenge append a signed issuance.log line. After remaining life that write is refused. POST /verifier-challenge and GET /instances stay allowed.

Rung 93 closed the member-two and threshold leftover after the seal write. Kernel add_issuer_member, set_issuer_threshold, and set_verify_threshold wrote issuer.json during remaining life. The host already refused those paths. The command line can skip the host. The kernel now refuses the same way as rotate.

Rung 94 closed the intended-export over-refuse after remaining life. sign_issuance_log_tree_head refused after remaining life and blocked seal-export and kill-export. Those exports must still produce a document a verifier can accept. Kill after remaining life was already allowed. Mint, birth, spawn, present, check, challenge, rotate, member-two, and both thresholds stay refused.

Rung 95 locked the remaining verifier artifacts after remaining life. Act-export of a historical receipt, previous-key-export, and GET /issuer-public were already allowed. Named allow tests now prove those paths. A sealed issuing store still cannot pin a foreign issuer public key. Issuing-host start after remaining life stays refused. That is a start gate, not a per-path over-refuse.

Hunt after Rung 95 (Rung 96 candidate, proof only, 23 August 2026). prepare_host_store (host.rs:2705) calls require_issuer_not_sealed when the host is not check-only. require_issuer_not_sealed refuses when is_sealed_at(now) is true. CLI `prometheus host` calls run_host_from_flags, then run_host_with_mode, then prepare_host_store. A fresh issuing-host start after remaining life refuses. Existing test check_only_host_starts_without_a_live_mint_issuer_and_stays_loopback (host.rs:5642) already expects that refuse, and allows check-only start. SOURCE-OF-TRUTH.md and ANATOMY.md name GET / as the later user interface. They name export, kill, and listing after remaining life as path allows. They do not name issuing-host start after remaining life as a required allow or a required refuse. JUDGE-NON-CLI.md says host start is a listen command so the page exists. That page does not require start after remaining life. A host that is already running can export after remaining life (Rung 94 and Rung 95). Command-line export after remaining life already works. This hunt does not unlock start. This hunt does not invent a product lock. Rust was not changed. The laboratory test count stays 520 tests.

Rung 96 closed member two remote path over site-to-site. A throwaway issuing store wrote member two through an SSHFS mount of prometheus-member-two. Birth without that path refused. Birth with --member-secret on the mount succeeded. Public check was not done. Rust was not changed. The laboratory test count stays 520 tests.

Rung 97 closed the public allow-then-refuse on that remote path. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. POST /check-svid allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not used. Rust was not changed. The laboratory test count stays 520 tests.

Rung 98 closed later user interface Create Agent Principal on that remote path. GET / on 127.0.0.1:18832 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth with the path after fusermount refused. Birth and present-svid with the remounted path succeeded with two member signatures. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-member-two-remote.

Rung 99 closed later user interface Create Agent Principal and Assertion Act on that remote path, then public allow then refuse. GET / on 127.0.0.1:18834 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-svid with the remote path succeeded with two member signatures. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-svid allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-member-two-public.

Rung 100 closed later user interface Create Agent Principal and WIMSE Assertion Act on that remote path, then public allow then refuse. GET / on 127.0.0.1:18836 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-wimse with the remote path succeeded with two member signatures. The Workload Identity Token subject is the present-hash wimse Uniform Resource Identifier. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-wimse allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-member-two-public-wimse.

Rung 101 closed laboratory operator page Create Agent Principal on that remote path. GET /laboratory on 127.0.0.1:18838 is the laboratory operator page. GET / on the same host is the later user interface and is a different page. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth with the path after fusermount refused. Birth and present-svid with the remounted path succeeded with two member signatures. Public check of this present was not done. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-laboratory-member-two-remote.

Rung 102 closed laboratory operator page Create Agent Principal and Assertion Act on that remote path, then public allow then refuse. GET /laboratory on 127.0.0.1:18842 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-svid with the remote path succeeded with two member signatures. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-svid allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-laboratory-member-two-public.

Rung 103 closed laboratory operator page Create Agent Principal and WIMSE Assertion Act on that remote path, then public allow then refuse. GET /laboratory on 127.0.0.1:18844 posted the same HTTP JSON the page posts. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-wimse with the remote path succeeded with two member signatures. The Workload Identity Token subject is the present-hash wimse Uniform Resource Identifier. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. Holder-sign ran locally. POST /check-wimse allowed. After Decommission, kill export, and public kill-accept, the same historical Assertion Act refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/later-ui-laboratory-member-two-public-wimse.


Rung 104 closed `prometheus runtime-check agent-process` on hostname 5090 with the remote member-secret path, then public allow then refuse. Create Agent Principal stayed on 127.0.0.1:18846. POST /member-two wrote member two through the SSHFS mount. Birth without the path refused. Birth and present-svid with the remote path succeeded with two member signatures. POST /issuer-accept pinned the throwaway public key only on https://check.prestigeworldwide.digital. LaboratoryRuntime still refused --holder-secret-path. Agent-process stayed up as process identifier 3559917. The first tool line printed ALLOWED and ran the tool. After Decommission, kill export, and public kill-accept, a second tool line on that same process identifier printed REFUSED because this store accepted a kill. The tool did not run. The process did not restart. Hermes was not started. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not raised. issuer.secret stayed off the VPC. Rust was not changed. The laboratory test count stays 520 tests. The walk lives in see-walk/member-two-agent-process.

Rung 105 closed the issuer-threshold raise leftover. Kernel set_issuer_threshold saved issuer.json at the new n, then signed the issuance.log line at that new n. When the outside member secret was not presented, the sign refused after n=2 was already persisted. The command line can skip the host. The kernel now checks member secrets for the new n before any write. A refused raise leaves threshold_n at 1. Same-n after n=2 stays a no-op. The laboratory test count is 522 tests.

Rung 106 closed the in-directory member-two write leftover. Host POST /member-two already required a typed outside path. Kernel add_issuer_member and a missing --secret-path still wrote issuer-member-*.secret under the data directory. The kernel now refuses a missing member-two secret path. A path inside the data directory was already refused. Stolen store files must not include member two. Default in-directory member files still load without --member-secret. The command line requires --secret-path. This is not n=3. This is not restore. The laboratory test count is 523 tests.

Rung 107 closed the planted lower threshold_n leftover on unsigned issuer.json. load_issuer treated that file n as live. issuance.log already stores the signed n on each line. A stolen n=2 store plus a planted threshold_n of 1 let mint, birth, and save-sign use only issuer.secret. Member two was not needed. The kernel now treats the highest signed log n as live when the file n is lower. A planted lower n is not live. save_issuer refuses a persist below that signed n. This is not a sixth identity record. This is not restore. This is not n=3. The laboratory test count is 524 tests.

Rung 108 closed the planted cleared kill_date leftover on unsigned issuer.json. load_issuer treated a missing kill_date as live. A signed issuer_seal issuance.log line already records the seal. A stolen sealed store plus a planted cleared kill_date let mint and birth treat the issuer as live. The kernel now treats a signed issuer_seal line as live when the file kill_date is missing. A planted clear is not live. save_issuer refuses a persist that clears kill_date after that signed line. This is not a sixth identity record. This is not restore. This is not Sanctum. The laboratory test count is 525 tests.

Rung 109 closed the planted later kill_date leftover on unsigned issuer.json. load_issuer treated a later file kill_date as live. A signed issuer_seal issuance.log note already stores issuer.kill_date. A stolen sealed store plus a planted later kill_date let mint and birth continue after the signed death. The kernel now treats the earliest signed issuer.kill_date in those notes as live when the file date is later. A planted later date is not live. save_issuer freezes against that signed date. This is not a sixth identity record. This is not restore. This is not Sanctum. The laboratory test count is 526 tests.

Rung 110 closed the planted later previous-key kill_date leftover on unsigned issuer.json. load_issuer treated a later previous_issuer_keys kill_date as live. A signed issuer_rotate issuance.log note already stores previous_public_key and kill_date. A stolen rotated store plus a planted later previous-key kill_date let a stolen old key stay trusted after the signed death. The kernel now treats the earliest signed previous-key kill_date in those notes as live when the file date is later. A planted later date is not live. save_issuer freezes against that signed date. This is not a sixth identity record. This is not restore. This is not Sanctum. The laboratory test count is 527 tests.

Rung 111 closed the planted lower verify_threshold_n leftover on unsigned issuer.json. load_issuer treated that file verify_threshold_n as live. A signed issuer_verify_threshold issuance.log note already stores the raised n. A stolen store plus a planted verify_threshold_n of 1 let a one-signature foreign act verify. The kernel now treats the highest signed verify_threshold_n in those notes as live when the file n is lower. A planted lower n is not live. save_issuer refuses a persist below that signed n. This is not issuance threshold_n. This is not a sixth identity record. This is not restore. This is not Sanctum. The laboratory test count is 528 tests.

Rung 112 closed the planted drop of previous_issuer_keys leftover on unsigned issuer.json. load_issuer overlaid a later previous-key kill_date only when that hex was still in the file. A signed issuer_rotate issuance.log note already stores previous_public_key and kill_date. A stolen rotated store plus a planted empty previous_issuer_keys list let a stolen old key stay trusted after the signed death. The kernel now restores a missing signed previous key in memory when that rotate note exists. A planted drop is not live. save_issuer refuses a persist that omits a signed previous key. This is not a sixth identity record. This is not restore. This is not Sanctum. The laboratory test count is 529 tests.

Rung 113 closed laboratory cold restore backup of one issuer. Jason Gale unlocked Structure A for laboratory testing. After the old mint is dead, an operator carries a backup of issuer.secret plus the ledger onto an empty issuing store. Member two stays outside that backup. The issuer public key does not change. Restore onto a store that already has an issuer is refused. A backup path inside the data directory is refused. Standing data-a was not the restore target. Restore is restore. Restore is disaster recovery of the same issuer. Restore is not a separate product. This is not a second live Create Agent Principal. This is not copying issuer.secret onto a live Store B. This is not Sanctum. The laboratory test count is 534 tests.

Rung 114 closed restore diagnostics after cold restore. Jason Gale locked self-healing after failure as internal diagnostics that run after restore. Those diagnostics indicate whether restore succeeded and operation returned to normal. They do not invent issuer.secret. They do not start a second Create Agent Principal. restore_from_backup now runs those checks and refuses when operation is not normal. Self-healing after failure is no longer an undefined open. It is post-restore diagnostics. Restore is restore. Restore is disaster recovery of the same issuer. Restore is not a separate product. Standing data-a was not the restore target. This is not Sanctum. The laboratory test count is 536 tests.

Rung 115 closed signed previous-key accept on Store B. Store B accept_previous_issuer_key wrote only issuer.json. A stolen verifier plus a planted later or dropped accepted_previous_issuer_keys kill date let a stolen old issuer key stay trusted after accepted death. The kernel now appends a signed previous_key_accept issuance.log line and treats that signed kill date as live. This leftover is closed. Kill-accept, seal-accept, and act-accept still write no second issuance.log line. Restore stays restore. This is not Sanctum. The laboratory test count is 538 tests.

Rung 116 closed laboratory issuance threshold 3. A third outside member may be added. set_issuer_threshold(3) succeeds when three members exist. Two members cannot raise n to 3. Birth at n=3 needs three secrets. The host no longer refuses a third outside member. POST /set-issuer-threshold may take n=3. Confirm stays the exact word issuer-threshold. Member three is not copied into a backup. Standing data-a stays issuance threshold_n 2. This is not SPIRE. The laboratory test count is 543 tests.

Rung 117 closed the command-line holder challenge after instance Decommission leftover. Host POST /challenge already refused a challenge for a revoked instance. Kernel issue_holder_challenge loaded the instance and appended a signed challenge issuance.log line with no live check. The command line Challenge path calls the kernel directly. The command line can skip the host. The kernel now refuses when the instance is not live. A missing instance is refused. A refused challenge does not append a new challenge line. sign_holder_nonce after local kill was already refused. This leftover is closed. This is not a sixth identity record. The laboratory test count is 544 tests.

Rung 118 started the loopback restore surface. GET / and GET /laboratory post POST /backup, POST /restore, and POST /diagnose on the issuing loopback host. Confirm must equal the exact word backup or restore. Check-only and the public well-known document omit those writes. Restore onto a dest that already has an issuer is refused. An issuing host with empty --data may start so restore can write. Standing data-a is not the restore dest. Secret bytes are not returned. The laboratory test count is 549 tests.

Rung 119 closed present after cold restore on the same issuer pin. After restore the issuer public key is unchanged. An honest present from a restored live instance verifies. A Store C that pinned the original public key allows that present. After kill-accept that same present refuses. GET / already posts backup, restore, and diagnose. Standing data-a is not the restore dest. This is not Sanctum. The laboratory test count is 552 tests.

Rung 120 closed the outside member after cold restore at threshold 2. Standing data-a is issuance threshold_n 2. After restore onto an empty dest, restore diagnostics report operation_normal. The issuer public key does not change. Backup still excludes member two. Birth and present on dest without the outside member refuse. Birth and present with the same outside member-two path succeed. A verifier that pinned the original public key allows that dest present. GET / already posts backup, restore, and diagnose. Standing data-a stays n=2. Standing data-a is not the restore dest. This is not Sanctum. The laboratory test count is 554 tests.

Rung 121 closed WIMSE present after cold restore on the same issuer pin. After restore the issuer public key is unchanged. An honest WIMSE present from a restored live instance verifies. A Store C that pinned the original public key allows that Workload Identity Token. After kill-accept that same WIMSE present refuses. GET / already posts backup, restore, and diagnose. This is the second on-ramp. This is not a third presenter. This is not SPIRE. Standing data-a is not the restore dest. This is not Sanctum. The laboratory test count is 556 tests.

Rung 122 closed the two-host cold-restore see-walk. Host A on 127.0.0.1 uses empty-then-inited throwaway --data and GET /laboratory. Host A writes a backup to an outside /tmp path. Host B starts with empty --data on another loopback port, restores, and diagnoses. Diagnose reports operation_normal. Host B can present-svid and present-wimse. Restore on Host A refuses because that dest already has an issuer. At n=2 dest birth and present without the outside member path refuse. Standing data-a is not the restore dest. The walk lives in see-walk/cold-restore-two-host. This is not Sanctum. The laboratory test count is 558 tests.

Rung 123 closed the outside members after cold restore at threshold 3. Standing data-a stays issuance threshold_n 2. After restore onto an empty dest, restore diagnostics report operation_normal. Backup still excludes member two and member three. Dest birth without outside members refuses. Dest birth with only one outside member refuses. Dest birth after both outside member paths are registered succeeds. A verifier that pinned the original public key allows that dest present. GET / already posts backup, restore, and diagnose. This is not SPIRE. This is not Sanctum. The laboratory test count is 560 tests.

Rung 124 closed the planted extra public_keys leftover on unsigned issuer.json. save_issuer already refused grow. load_issuer did not shrink extras. A planted extra was a live verify key. load_issuer now drops extras that are not the current public key and not a signed issuer_member_add public key. Do not write the file. Honest signed member keys stay. Standing data-a stays issuance threshold_n 2. This leftover is closed. This is not a sixth identity record. The laboratory test count is 562 tests.

Rung 125 closed the planted current_public_key leftover on unsigned issuer.json. save_issuer already refused a current key that does not match issuer.secret. load_issuer still treated a planted current as a live verify key. A planted current also kept a matching public_keys extra after Rung 124. load_issuer now sets the in-memory current public key to the issuer.secret public key when they differ. Do not write the file. A planted current is not a live verify key. Standing data-a stays issuance threshold_n 2. This leftover is closed. This is not a sixth identity record. The laboratory test count is 564 tests.

The public check name is check.prestigeworldwide.digital. That host is check only. Create Agent Principal stays on 127.0.0.1.

See JUDGE-NON-CLI.md for what a person can prove without the command line. Init stays on the command line. Host start is a listen command.

This page does not say the bet is won.

GitHub publication started and landed on 23 August 2026. The public repository is https://github.com/jwgale/prometheus-identity. The repository name is the Cargo package name prometheus_identity. That name is not a market name. issuer.secret, biscuit secrets, holder secrets, member-two secrets, data directories, and prometheus-lab-vpc were not published. GitHub Actions were not added.

Standing data-a cutover is done on hostname 5090. The standing store is issuance threshold_n 2, member_count 2, verify_threshold_n 1, sealed no. Member two was written as a new secret through --secret-path /home/jason/Projects/prometheus-lab-vpc/mnt-member-two/member-two.secret. The laptop file /home/jason/Projects/prometheus-lab-vpc/member-two.secret was not copied. That laptop file mtime remains 19 August 2026 12:24 CDT. A raise without --member-secret refused and left threshold_n at 1. That is Rung 105 on the live store. A raise with the mount path set threshold_n to 2. An agent type written at n=1 refused at birth even with the path. New agent type 01M0RPB06MQYYK1BFYPSNEBZSH was added with the path. Birth without the path refused. Birth with the path allowed. The proof instance is 01M0RPC3Q0MMQ2BBA0BKJMXXAK. Kill without the path refused. Kill with the path revoked that proof instance. Standing instance 01M0D6XG76SFGNZRSNFM8C7297 stays live. data-a was not shredded. After this cutover, standing laboratory mint is broken unless the member-two mount is up. Do not raise threshold again. Do not raise verify_threshold_n.

## Parked

SPIRE is parked. SPIRE starts only as a consumer when a runtime will not call loopback /check-svid or https://check.prestigeworldwide.digital. Do not start SPIRE.

Do not invent more presenter-pair walks. Do not polish Birth, Present, and Death labels on GET /laboratory.

## Next unfinished

Laboratory cold restore backup started as laboratory Structure A as Rung 113. Restore diagnostics after cold restore closed as Rung 114. Self-healing after failure is no longer an undefined open. It is post-restore diagnostics. Restore is restore. Restore is disaster recovery of the same issuer. Restore is not a separate product. Store B signed previous-key accept closed as Rung 115. The planted drop of previous_issuer_keys in unsigned issuer.json is closed as Rung 112. The planted lower verify_threshold_n in unsigned issuer.json is closed as Rung 111. The planted later previous-key kill_date in unsigned issuer.json is closed as Rung 110. The planted later kill_date in unsigned issuer.json is closed as Rung 109. The planted cleared kill_date in unsigned issuer.json is closed as Rung 108. The planted lower threshold_n in unsigned issuer.json is closed as Rung 107. The in-directory member-two write is closed as Rung 106. Kill-accept, seal-accept, and act-accept still write no second issuance.log line. Laboratory n=3 closed as Rung 116. The command-line holder challenge after instance Decommission leftover is closed as Rung 117. Loopback restore surface started as Rung 118. Present after cold restore on the same issuer pin closed as Rung 119. The outside member after cold restore at n=2 closed as Rung 120. WIMSE present after cold restore on the same issuer pin closed as Rung 121. The two-host cold-restore see-walk closed as Rung 122. Outside members after cold restore at n=3 closed as Rung 123. The planted extra public_keys leftover on unsigned issuer.json closed as Rung 124. The planted current_public_key leftover on unsigned issuer.json closed as Rung 125. Standing data-a stays n=2. Remaining leftovers stay parked or are not coding slices: SPIRE, Check-both matrix, issuing-host start after remaining life, /laboratory label polish, stolen-issuer close, authorization talk, market holder, kill-accept, seal-accept, and act-accept second log line, a signed issuer-key accept line on check-only Store C, and a later full header-coverage HTTP Message Signatures stack. That later stack is not a startable fail-closed leftover without opening more headers than the locked method, request-target, and content-digest bind. Do not invent a market holder for member two. Do not start SPIRE. Standing data-a cutover is done. Do not raise threshold again. Do not unlock issuing-host start after remaining life. Do not invent another operator-page pairing.

VISION-open items that are not coding slices: who holds member two in a later market. Restore is restore. Self-healing after failure is post-restore diagnostics. Laboratory cold restore backup started as laboratory Structure A. Standing data-a was not the restore target. How a later market names the well-known check was locked on 26 August 2026: the path stays GET /.well-known/prometheus-check as a protocol constant. A later market may name the host and may later lock a product word for Check. Check stays unnamed. The stolen-issuer window was locked on 26 August 2026 as remaining life until Store B accepts seal or a previous-key kill. It is not a kernel leftover. A later market may require seal-accept before issuer Decommission is complete. The kernel does not push. No public transparency log. One kernel, restorable was locked on 26 August 2026. Create Agent Principal has one birth authority. A second live issuer is a second identity kernel and is refused. Recovery is restore of that same issuer after the issuing computer is dead. Restore is not copying issuer.secret onto a live Store B. Restore is restore. Restore is disaster recovery of the same issuer. Restore is not a separate product.

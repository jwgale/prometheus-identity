Laboratory operator page remote member-secret path WIMSE public allow then refuse

Date: 23 August 2026 18:46 America/Chicago

This folder is for Jason Gale. These files are public records and artifacts. This folder does not contain issuer.secret, biscuit.secret, holder secrets, or member-two secrets. The issuing store lived under /tmp/prometheus-later-ui-laboratory-member-two-public-wimse-20260823 and was shredded after this walk.

GET /laboratory on 127.0.0.1:18844 is the laboratory operator page. The title is Prometheus loopback operator page. GET / on the same host is the later user interface and is a different page. The walk used the same HTTP JSON that GET /laboratory posts. Init stayed on the command line. Host start was a listen command. Holder-sign ran locally. The walk did not spawn AgentProcess.

POST /member-two wrote member two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. Secret bytes were not returned. The standing laptop member-two.secret was not copied. issuer.secret stayed on the issuing computer. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /agent-type used member_secret_path after that raise.

POST /birth without member_secret_path returned 403 and named that required path. POST /birth with the remote path returned 200. POST /challenge and POST /present-wimse with the same remote path returned 200 and two issuer member signatures. The Workload Identity Token subject is the present-hash wimse Uniform Resource Identifier. The instance identifier is not in that subject.

GET https://check.prestigeworldwide.digital/.well-known/prometheus-check returned bind check.prestigeworldwide.digital and named /check-wimse. POST /issuer-accept pinned this throwaway public key only. Holder-sign ran locally. POST /check-wimse allowed. The check-wimse body used the same request keys as later-ui-member-two-public-wimse and later-ui-laboratory-public-wimse.

After Decommission and POST /kill-export with member_secret_path, POST /kill-accept on the public name returned 200. The accepted kill list includes instance 01M0RG5N7MG3FZAS9B0EJRB2EK. A following POST /check-wimse of the same historical Assertion Act returned 403 because this store accepted a kill, not because of expiry.

The throwaway store was shredded. The VPC custody directory was left in place. Standing data-a threshold_n stayed 1. The laptop member-two.secret mtime stayed 19 August 2026 12:24 CDT. Mount issuer.secret count stayed 0. Direct SSH issuer.secret count stayed 0. Rust was not changed.

This is not a public listener for birth. This is not SPIRE. This is not a replica. This is not a sixth identity record. This is not Sanctum.

issuing GET / is the later user interface
GET / already posts POST /rotate, POST /previous-key-export, and POST /previous-key-accept
listen 127.0.0.1:18808
content_type text/html; charset=utf-8
html_bytes 89489
markers id="check-base", name="check_base", https://check.prestigeworldwide.digital, Create Agent Principal, /runtime-check, The holder secret path stays on this host, POST /runtime-check signs the verifier nonce here, The path is not sent to the check base, Check again, id="rotate-confirm", id="rotate-issuer", id="export-previous-key", id="accept-previous-key", Rotate the issuer, postJson("/rotate", postJson("/previous-key-export", postJson("/previous-key-accept", Type the word rotate to confirm

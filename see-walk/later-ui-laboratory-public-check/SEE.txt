Laboratory operator page public typed-base Check again walk

Date: 23 August 2026 02:50 America/Chicago

This folder is for Jason Gale. These files are public records and artifacts. This folder does not contain issuer.secret, biscuit.secret, holder secrets, or member-two secrets. The issuing store lived under /tmp/prometheus-later-ui-laboratory-public-check-a.

The issuing host listened on 127.0.0.1:18818 only and is stopped after this walk. GET /laboratory on that host is the laboratory operator page. Check again was missing on that page and is now present. The page types a check base. The accepted public base is https://check.prestigeworldwide.digital.

The walk used the same HTTP JSON that GET /laboratory Check and Check again post. The walk did not use prometheus CLI check verbs. The walk did not spawn AgentProcess.

Create Agent Principal stayed on 127.0.0.1. POST /well-known-follow of the public check base resolved issuer-accept and kill-accept from operator_pin_paths. POST /operator-pin issuer-accept on the issuing host returned 200. This throwaway issuer was not already pinned. issuer.secret was not copied onto the public host. The walk did not hardcode public /kill-accept as the accept URL.

POST /runtime-check on the loopback issuing host with check_base https://check.prestigeworldwide.digital allowed. Check again of that same present also allowed. ALLOWED was not stored. Each post hit the host. The holder secret path stayed on the issuing host. Secret bytes were not sent to the public name.

After Decommission, POST /kill-export on the issuing host returned the three public artifacts. POST /operator-pin kill-accept returned 200. A following Check again POST /runtime-check of the same present returned 403. The reason is accepted kill, not expiry and not a missing inode.

A following POST /check-svid of that same historical Assertion Act on the public host also returned 403 because this store accepted a kill.

Live GET https://check.prestigeworldwide.digital stayed check-only JSON. Live POST /runtime-check, POST /operator-pin, and POST /well-known-follow on that name stayed 403.

GET /laboratory does not claim Check both or a named act. Those controls were not added.

This is not SPIRE. This is not a replica. This is not Sanctum.

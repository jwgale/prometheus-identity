issuing GET / is the later user interface
GET / already posts POST /seal and POST /seal-export
listen 127.0.0.1:18806
content_type text/html; charset=utf-8
html_bytes 89489
markers id="check-base", name="check_base", https://check.prestigeworldwide.digital, Create Agent Principal, /runtime-check, The holder secret path stays on this host, POST /runtime-check signs the verifier nonce here, The path is not sent to the check base, Check again, id="seal-confirm", id="seal-issuer", id="export-seal-bundle", Seal the issuer, postJson("/seal", postJson("/seal-export", Type the word seal to confirm

# Prometheus judge page

Date: 20 August 2026.

This page is for Jason Gale. This package is laboratory code under PolicyLab-2. This package is not Sanctum. This package is not a Cyera product.

This document uses ASD-STE100 Simplified Technical English. Technical names appear in full words.

The visible walk lives in `see-walk/judge-rung6`. The two stores lived under `/tmp/prometheus-judge-rung6`. That walk directory holds public artifacts only. It does not hold `issuer.secret`, `biscuit.secret`, holder secrets, or member-two secrets.

The host listened on `127.0.0.1:18771` only. The host is stopped.

## Five questions

1. Can birth write the instance, the first capability, and death in one persist? **yes**
2. Can a second party verify an act without copying the inode? **yes**
3. Can death travel to that second party and refuse a later present? **yes**
4. Can a runtime consume that present without becoming a directory? **yes**
5. Does a stolen or resigned wrap fail closed after kill? **yes**

All five answers are yes. The kernel is now testable. This page does not say the bet is won.

20 August 2026. The loopback consumer, portable death, traveling seal, and threshold-two birth are now visible. The five original answers stay yes. This page does not say the bet is won.

## One sentence each

1. One `birth` persist wrote the live instance, the first capability, the revoke identifier, and one `birth_write` issuance-log line.
2. Store B accepted the store A public key and the act bundle. Store B wrote no instance record and no issuance-log line.
3. Store B accepted the kill bundle. Store B then refused `present verify-svid` of the historical wrap. The store A host also refused that wrap after local kill.
4. The loopback host allowed POST `/check-svid` for the honest wrap. The host is a check. The host is not a directory.
5. After kill, a wrap resigned with a foreign Ed25519 key was posted to the same host. The host returned HTTP 403 and signed no receipt.

## Artifact paths

All paths below sit under `see-walk/judge-rung6`.

- Question 1: `birth.json`, `issuance-after-birth.log`, `capability-public.json`
- Question 2: `b-issuer-accept.json`, `b-act-accept.json`, `b-no-mint.txt`, `act-receipt.json`, `act-proof.json`, `act-tree-head.json`
- Question 3: `a-kill.json`, `kill-event.json`, `b-kill-accept.json`, `check-svid-after-kill.json`, `b-verify-svid-after-kill.txt`, `instance-after-kill.json`
- Question 4: `presentation.json`, `presentation.json.svid.pem`, `check-svid-allowed.json`, `present-emit.json`
- Question 5: `resigned.pem`, `check-svid-resigned.json`

## Blocked work

No command was blocked. A first openssl empty-subject config failed. A second openssl command with matching NotAfter failed. The live resign used a foreign Ed25519 key and a one-day NotAfter. The host refused that wrap after kill. The tests were not re-run.

## Saturday 22 August 2026

This page was dated 20 August 2026. The five original answers stay yes. These Saturday walks closed after that date. Each walk has a public SEE.txt. This page does not invent test counts for those walks. This page does not say the bet is won.

- Public check name and check-only host: the public verifier is https://check.prestigeworldwide.digital, honest before-tool allowed, and after Decommission the public host refused from accepted kill. See see-walk/public-check-before-tool/SEE.txt.
- Hermes one-shot before-tool: the agent on Hermes ran prometheus runtime-check before-tool with holder-sign, honest allow printed ALLOWED and ran the tool, and after Decommission the same historical Assertion Act printed REFUSED. See see-walk/hermes-agent-before-tool/SEE.txt.
- Hermes child cascade: spawn wrote a narrower child, and after parent Decommission the child Assertion Act on Hermes printed REFUSED because this store accepted a kill cascade. See see-walk/hermes-child-cascade/SEE.txt.
- Public seal-accept stolen issuer: after public seal-accept, a stolen issuer.secret mint was refused on the public host for that same pin. See see-walk/public-seal-accept/SEE.txt.

## Rung 51 — durable agent process

22 August 2026. `prometheus runtime-check agent-process` stayed up on Hermes as process identifier 40356. The first tool line printed ALLOWED and ran the tool. After Decommission by kill accept, a second tool line on that same process identifier printed REFUSED because this store accepted a kill. The tool did not run. The process did not restart. See see-walk/hermes-agent-process/SEE.txt.

The laboratory test count at this close is 456 tests. This page does not say the bet is won.

## Rung 52 — public-check fail-closed leftovers

22 August 2026. The public check-only host refused Create Agent Principal, spawn, and Assertion Act mint. AgentProcess allowed then refused after seal accept on the same process identifier. AgentProcess allowed a child then refused after parent kill accept on the same process identifier. The laboratory runtime refused a well-known document that named a write verb as a check path. GET https://check.prestigeworldwide.digital/.well-known/prometheus-check returned bind check.prestigeworldwide.digital. VISION section 7 now starts SPIRE only when a real runtime will not call loopback /check-svid or https://check.prestigeworldwide.digital.

The laboratory test count at this close is 460 tests. This page does not say the bet is won.

## Rung 53 — public well-known check honesty

22 August 2026. A public check-only well-known document names bind check.prestigeworldwide.digital and the existing check paths. That document does not name Create Agent Principal, spawn, Assertion Act mint, or export write verbs. The laboratory runtime follows the public document and refuses a document that points a check path at a write verb. Loopback still names operator export pins. GET https://check.prestigeworldwide.digital/.well-known/prometheus-check still serves the previous public process document until that process is restarted with this binary. See see-walk/public-well-known-honesty/SEE.txt.

The laboratory test count at this close is 461 tests. This page does not say the bet is won.

## Rung 54 — public WIMSE sibling of the live SVID walk

22 August 2026. The public check-only process was restarted. GET https://check.prestigeworldwide.digital/.well-known/prometheus-check now omits export write verbs. `prometheus runtime-check agent-process` stayed up on Hermes as process identifier 42541 with a WIMSE Assertion Act. The first tool line printed ALLOWED and ran the tool. After Decommission by kill accept, a second tool line on that same process identifier printed REFUSED because this store accepted a kill. The tool did not run. The process did not restart. See see-walk/public-wimse-agent-process/SEE.txt.

The laboratory test count at this close is 463 tests. This page does not say the bet is won.

## Rung 55 — multi-act SVID and WIMSE in one durable process

22 August 2026. `prometheus runtime-check agent-process` stayed up on Hermes as process identifier 48101 with both a laboratory X.509-SVID wrap and a WIMSE Assertion Act. The first tool line printed ALLOWED only after both documented checks allowed, and ran the tool. After Decommission by kill accept, a second tool line on that same process identifier printed REFUSED because this store accepted a kill. The tool did not run. The process did not restart. Live public write verbs stayed refused. See see-walk/public-multiact-agent-process/SEE.txt.

The laboratory test count at this close is 465 tests. This page does not say the bet is won.

## Rung 56 — add a later Assertion Act without a restart

23 August 2026. `prometheus runtime-check agent-process` stayed up on Hermes as process identifier 57668 with one parent X.509-SVID Assertion Act. The first tool line printed ALLOWED and ran the tool. An honest add-act of a narrower child printed ADDED on that same process identifier. A later tool line printed ALLOWED only after both documented checks allowed, and ran the tool. After parent Decommission by kill accept, a later tool line on that same process identifier printed REFUSED because this store accepted a kill cascade. The tool did not run. The process did not restart. A mixed on-ramp add-act line and an add-act line that named a holder secret path printed REFUSED. See see-walk/public-add-act-agent-process/SEE.txt.

The laboratory test count at this close is 468 tests. This page does not say the bet is won.

## Rung 57 — named act when only one of two held acts is live

23 August 2026. `prometheus runtime-check agent-process` stayed up on Hermes as process identifier 59823 with two independent X.509-SVID Assertion Acts. An unnamed tool line printed ALLOWED only after both checks allowed. After Decommission of the first instance by kill accept, an unnamed tool line on that same process identifier printed REFUSED because this store accepted a kill. A line that named act 2 printed ALLOWED and ran the tool. A line that named act 1 printed REFUSED because this store accepted a kill. The process did not restart. See see-walk/public-named-act-agent-process/SEE.txt.

The laboratory test count at this close is 470 tests. This page does not say the bet is won.

## Rung 58 — one-shot act and before-tool refuse a mixed on-ramp

23 August 2026. `prometheus runtime-check act` and `prometheus runtime-check before-tool` name one on-ramp. Passing both a laboratory X.509-SVID wrap and a WIMSE present on the same one-shot line had silently used only the X.509-SVID wrap. That mix is now refused. Completing both checks stays the durable agent-process path. Honest one-shot X.509-SVID and honest one-shot WIMSE stay unchanged. No live walk. The published well-known document did not change.

The laboratory test count at this close is 471 tests. This page does not say the bet is won.

## Rung 59 — add-act of a WIMSE child

23 August 2026. `prometheus runtime-check agent-process` stayed up on Hermes as process identifier 65061 with one parent X.509-SVID Assertion Act. The first tool line printed ALLOWED. An honest add-act of a narrower WIMSE child printed ADDED on that same process identifier. A later tool line printed ALLOWED only after both documented checks allowed, and ran the tool. After parent Decommission by kill accept, a later tool line on that same process identifier printed REFUSED because this store accepted a kill cascade. The tool did not run. The process did not restart. A mixed on-ramp add-act line and an add-act line that named a holder secret path printed REFUSED. See see-walk/public-add-act-wimse-child/SEE.txt.

The laboratory test count at this close is 472 tests. This page does not say the bet is won.

## Rung 60 — named act on a WIMSE-held independent second Assertion Act

23 August 2026. `prometheus runtime-check agent-process` stayed up on Hermes as process identifier 76184 with one X.509-SVID Assertion Act and one independent WIMSE Assertion Act. An unnamed tool line printed ALLOWED only after both checks allowed. After Decommission of the first instance by kill accept, an unnamed tool line on that same process identifier printed REFUSED because this store accepted a kill. A line that named act 2 printed ALLOWED and ran the tool. A line that named act 1 printed REFUSED because this store accepted a kill. After Decommission of the second instance, a line that named act 2 printed REFUSED. Act 0, a missing act number, and an act number this process does not hold are refused. The process did not restart. See see-walk/public-named-act-wimse/SEE.txt.

The laboratory test count at this close is 473 tests. This page does not say the bet is won.

## Rung 61 — durable well-known pin

23 August 2026. `prometheus runtime-check agent-process` pins the first honest GET /.well-known/prometheus-check document for the life of the process. A later GET that changes bind, changes a check path, or grows the checks list is refused. A missing document after that first honest fetch is refused. A restored honest document may proceed. The process still requests a fresh verifier challenge and a fresh holder proof on every tool line. ALLOWED is not cached. No live walk: the lock does not change agent behavior against the honest public check name. The published well-known document did not change.

The laboratory test count at this close is 475 tests. This page does not say the bet is won.

## Rung 62 — one-shot well-known TOCTOU

23 August 2026. `prometheus runtime-check act` and `prometheus runtime-check before-tool` re-fetch the well-known document after the verifier challenge and before the documented check. A bind swap or a write-verb growth in that window is refused. The check is not posted. ALLOWED is not cached. No live walk: the lock does not change agent behavior against the honest public check name.

The laboratory test count at this close is 476 tests. This page does not say the bet is won.

## Rung 63 — well-known bind must match the accepted host

23 August 2026. LaboratoryRuntime and AgentProcess refuse a well-known document whose bind is not the host that start() or connect() accepted. http://127.0.0.1 and https://check.prestigeworldwide.digital are not interchangeable. A public bind on a loopback runtime is refused on the first fetch. No live walk: the honest public document already names the public bind.

The laboratory test count at this close is 477 tests. This page does not say the bet is won.

## Rung 64 — check HTTP 200 parse honesty

23 August 2026. LaboratoryRuntime and AgentProcess refuse a check HTTP status 200 whose JSON is refuse or denied. An empty 200 body is refused. HTTP 403 whose JSON is allowed is refused. The tool does not run. ALLOWED is not cached. No live walk: the honest public host already sends 200 only for allow. The published well-known document did not change.

The laboratory test count at this close is 479 tests. This page does not say the bet is won.

## Rung 65 — later user interface typed check base

23 August 2026. GET / Check types a check base. The accepted bases are http://127.0.0.1 on this host or https://check.prestigeworldwide.digital. This origin follows the well-known document. A different accepted base uses POST /runtime-check. That path drives LaboratoryRuntime. The issuing-store host does not become the agent and does not spawn AgentProcess. Holder secret bytes are not uploaded. An off-name base, HTTP to the public name, a missing holder proof, and holder secret bytes are refused. A check-only host refuses POST /runtime-check. POST /runtime-check allowed an honest X.509-SVID present against Store B, then refused the same present after Decommission and Store B kill-accept. Present bytes are not trimmed. ALLOWED is not cached. No live public walk: the public check host stays check-only JSON.

The laboratory test count at this close is 482 tests. This page does not say the bet is won.

## Rung 66 — later user interface Check again

23 August 2026. GET / Check again posts the same present to the typed check base without retyping. Each click hits the host. ALLOWED is not stored in the browser. After Decommission and Store B kill-accept, POST /runtime-check of that same present is refused. The issuing-store host does not spawn AgentProcess. No live public walk: the public check host stays check-only JSON.

The laboratory test count at this close is 483 tests. This page does not say the bet is won.

## Rung 67 — later user interface runtime-check leftover locks

23 August 2026. GET / Check and POST /runtime-check refuse www.prestigeworldwide.digital, the apex, a public port other than 443, a trailing-slash www variant, and HTTP to the public name with a port. A public check-only host refuses POST /runtime-check. GET / JavaScript does not fetch the public check host. WIMSE token and digest fields are not trimmed. Holder secret bytes stay off the page. Live GET https://check.prestigeworldwide.digital returned check-only JSON. Live POST /runtime-check on that name returned 403. See see-walk/later-ui-runtime-check-leftovers/SEE.txt.

The laboratory test count at this close is 484 tests. This page does not say the bet is won.

## Rung 68 — later user interface WIMSE typed-base Check again

23 August 2026. GET / Check WIMSE posts an off-origin typed base to POST /runtime-check. Check again posts the same WIMSE present. POST /runtime-check allowed an honest WIMSE present against Store B, then refused the same present after Decommission and Store B kill-accept. The issuing-store host does not spawn AgentProcess. No live public WIMSE walk: the public check host stays check-only JSON.

The laboratory test count at this close is 485 tests. This page does not say the bet is won.

## Rung 69 — later user interface Check both parent and child

23 August 2026. GET / holds two Assertion Acts after present. Spawn writes the child. Check both posts POST /runtime-check once per present. POST /runtime-check allowed the parent and the child against Store B. After parent Decommission and Store B kill-accept, both presents refused. A named check of the child also refused because this store accepted a kill cascade. A mixed on-ramp row, a missing holder path, an off-name base, HTTP to the public name, and holder secret bytes stayed refused. The issuing-store host does not spawn AgentProcess.

The laboratory test count at this close is 488 tests. This page does not say the bet is won.

## Rung 70 — later user interface named check of an independent live act

23 August 2026. GET / holds two independent Create Agent Principal presents. Check both posts POST /runtime-check once per present. After Decommission of the first instance and Store B kill-accept, Check both refuses. A named check of the live independent act still allows. A named check of the dead act refuses. Check both claims allow only when both host hits allow. The issuing-store host does not spawn AgentProcess.

The laboratory test count at this close is 489 tests. This page does not say the bet is won.


## Rung 71 — later user interface Check both SVID and independent WIMSE

23 August 2026. GET / holds one laboratory X.509-SVID Assertion Act and one independent WIMSE Assertion Act. Check both posts POST /runtime-check once per present. POST /runtime-check allowed both presents against Store B. After Decommission of the first instance and Store B kill-accept, Check both refuses. A named check of the live WIMSE act still allows. A named check of the dead X.509-SVID act refuses. First Decommission does not cascade-kill the WIMSE act. The issuing-store host does not spawn AgentProcess.

The laboratory test count at this close is 490 tests. This page does not say the bet is won.

## Rung 72 — later user interface Check both SVID allow plus WIMSE refuse

23 August 2026. Check both claims allow only when both host hits allow. A live X.509-SVID present plus a dead independent WIMSE present is not ALLOWED. POST /runtime-check still allows the live X.509-SVID act. POST /runtime-check refuses the dead WIMSE act. Mix on one row stayed refused. The issuing-store host does not spawn AgentProcess.

The laboratory test count at this close is 491 tests. This page does not say the bet is won.

## Rung 73 — later user interface Check both two independent WIMSE acts

23 August 2026. GET / holds two independent WIMSE Assertion Acts. Check both posts POST /runtime-check once per present. POST /runtime-check allowed both WIMSE presents against Store B. After Decommission of the first instance and Store B kill-accept, Check both refuses. A named check of the live independent WIMSE act still allows. A named check of the dead WIMSE act refuses. The issuing-store host does not spawn AgentProcess.

The laboratory test count at this close is 492 tests. This page does not say the bet is won.

## Rung 74 — later user interface Check both parent X.509-SVID and child WIMSE

23 August 2026. GET / holds a parent laboratory X.509-SVID wrap and a child WIMSE Assertion Act. Spawn writes the narrower child. Check both posts POST /runtime-check once per present. POST /runtime-check allowed the parent and the child against Store B. After parent Decommission and Store B kill-accept, Check both refuses. A named check of the child WIMSE also refuses because this store accepted a kill cascade. This is the last Check-both on-ramp sibling. The issuing-store host does not spawn AgentProcess.

The laboratory test count at this close is 493 tests. This page does not say the bet is won.

## Rung 75 — later user interface typed-base Check against the live public name

23 August 2026. GET / on 127.0.0.1:18805 is the later user interface. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest laboratory X.509-SVID present. The holder secret path stayed on the issuing host. Secret bytes were not sent to the public name. POST /issuer-accept on the public host was required for this throwaway store and returned 200. After Decommission and public kill-accept, the same POST /runtime-check refused because this store accepted a kill. A following POST /check-svid of that historical present on the public host also refused because this store accepted a kill. Live POST /runtime-check on the public name stayed 403. See see-walk/later-ui-public-runtime-check/SEE.txt.

The laboratory test count at this close is 493 tests. This page does not say the bet is won.

## Rung 76 — later user interface seal-accept against the live public name

23 August 2026. GET / on 127.0.0.1:18806 is the later user interface. GET / already posts POST /seal and POST /seal-export. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest laboratory X.509-SVID present. POST /issuer-accept on the public host was required for this throwaway store and returned 200. After local POST /seal with confirm seal and POST /seal-export, POST /seal-accept on the public host returned 200. The same POST /runtime-check refused because this store accepted a seal. A following POST /check-svid of that historical present on the public host also refused because this store accepted a seal. A stolen issuer.secret under /tmp minted a new Assertion Act. POST /runtime-check and POST /check-svid of that new present refused because this store accepted a seal for that issuer pin. The stolen copy and the throwaway store were deleted after the walk. Live POST /runtime-check on the public name stayed 403. See see-walk/later-ui-public-seal-accept/SEE.txt.

The laboratory test count at this close is 493 tests. This page does not say the bet is won.

## Rung 77 — later user interface previous-key-accept against the live public name

23 August 2026. GET / on 127.0.0.1:18808 is the later user interface. GET / already posts POST /rotate, POST /previous-key-export, and POST /previous-key-accept. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest laboratory X.509-SVID present signed by the old issuer key. POST /issuer-accept on the public host was required for this throwaway store and returned 200. After local POST /rotate with confirm rotate and POST /previous-key-export, POST /previous-key-accept on the public host returned 200. After the previous-key kill date, the same POST /runtime-check refused because the previous issuer key is past its kill date. A following POST /check-svid of that historical present on the public host also refused because the previous issuer key is past its kill date. A new present signed by the current issuer key still allowed. A stolen issuer.secret under /tmp minted a new Assertion Act on the old key. POST /runtime-check and POST /check-svid of that new present refused because the previous issuer key is past its kill date. The stolen copy and the throwaway store were deleted after the walk. Live POST /runtime-check on the public name stayed 403. See see-walk/later-ui-public-previous-key/SEE.txt.

The laboratory test count at this close is 493 tests. This page does not say the bet is won.

## Rung 78 — later user interface WIMSE typed-base Check against the live public name

23 August 2026. GET / on 127.0.0.1:18810 is the later user interface. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest WIMSE present. The holder secret path stayed on the issuing host. Secret bytes were not sent to the public name. POST /issuer-accept on the public host was required for this throwaway store and returned 200. After Decommission and public kill-accept, the same POST /runtime-check refused because this store accepted a kill. A following POST /check-wimse of that historical present on the public host also refused because this store accepted a kill. Live POST /runtime-check on the public name stayed 403. See see-walk/later-ui-public-wimse-runtime-check/SEE.txt.

The laboratory test count at this close is 493 tests. This page does not say the bet is won.

## Rung 79 — later user interface act-accept against the live public name

23 August 2026. GET / on 127.0.0.1:18812 is the later user interface. GET / already posts POST /act-export and POST /act-accept. Live POST /act-accept on https://check.prestigeworldwide.digital is an allowed operator pin. An empty body returned HTTP 400 missing field receipt. Live POST /act-export on that name stayed 403 check-only. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest laboratory X.509-SVID present. POST /issuer-accept on the public host was required for this throwaway store and returned 200. The issuing store also posted POST /check so the receipt stayed on the issuing store. POST /act-export on the issuing host returned the three public artifacts. POST /act-accept on the public host returned 200 accepted. Public GET /instances stayed 403. After Decommission and public kill-accept, the same POST /runtime-check refused because this store accepted a kill. A following POST /check-svid of that historical present on the public host also refused because this store accepted a kill. Live public pin leftovers refused truncated hexadecimal, empty public_key_hex, and empty accept artifacts. See see-walk/later-ui-public-act-accept/SEE.txt.

The laboratory test count at this close is 493 tests. This page does not say the bet is won.

## Rung 80 — public well-known operator-pin honesty

23 August 2026. Live empty-body POST on https://check.prestigeworldwide.digital returned HTTP 400 missing field for /issuer-accept, /kill-accept, /act-accept, /seal-accept, and /previous-key-accept. Those are allowed operator pins, not check-only 403. POST /verifier-challenge returned HTTP 200. Write and export verbs stayed 403. GET /.well-known/prometheus-check now names those five pins in operator_pin_paths. checks[] stays POST /check-svid and POST /check-wimse. verifier-challenge stays a named discovery field. Write and export verbs stay off the public document. Loopback still names operator export pins. The laboratory runtime follows that public document and refuses a document that points a check path at a write verb. The public check-only process was restarted. Process identifier 24205. See see-walk/public-well-known-operator-pins/SEE.txt.

The laboratory test count at this close is 494 tests. This page does not say the bet is won.

## Rung 81 — later user interface well-known operator-pin follow

23 August 2026. GET / verifier pin posts follow operator_pin_paths, checks[], and verifier-challenge from the typed check base well-known document. Same-origin follows this host document. Off-origin uses POST /well-known-follow and POST /operator-pin. The browser does not fetch the public name. www, the apex, HTTP to the public name, and a public port other than 443 stay refused. A missing pin is refused. A write-verb pin is refused. A check-only host refuses both helpers. Issuing-store write paths stay hardcoded on this store. The issuing-store host does not spawn AgentProcess. The published public well-known document did not change. Public host not restarted.

The laboratory test count at this close is 501 tests. This page does not say the bet is won.

## Rung 82 — later user interface operator-pin kill-accept against the live public name

23 August 2026. GET / on 127.0.0.1:18814 is the later user interface. POST /well-known-follow of check_base https://check.prestigeworldwide.digital resolved kill-accept from the public well-known document. POST /operator-pin issuer-accept on the issuing host returned 200. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest laboratory X.509-SVID present. After Decommission and POST /kill-export, POST /operator-pin kill-accept returned 200. The walk did not hardcode public /kill-accept as the accept URL. The same POST /runtime-check refused because this store accepted a kill. A following POST /check-svid of that historical present on the public host also refused because this store accepted a kill. Live public POST /operator-pin and POST /well-known-follow stayed 403. Issuing POST /operator-pin refused www, the apex, HTTP to the public name, and :8443. Live leftovers refused a write-verb pin name, a missing pin, empty accept artifacts, a missing receipt, and holder secret bytes. No live allow-hole. See see-walk/later-ui-public-operator-pin/SEE.txt.

The laboratory test count at this close is 501 tests. This page does not say the bet is won.

## Rung 83 — later user interface WIMSE act-accept via operator-pin against the live public name

23 August 2026. GET / on 127.0.0.1:18816 is the later user interface. POST /well-known-follow of check_base https://check.prestigeworldwide.digital resolved act-accept from the public well-known document. POST /operator-pin issuer-accept on the issuing host returned 200. POST /present-wimse minted a laboratory Workload Identity Token plus Content-Digest. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed that honest WIMSE present. The issuing store also posted POST /check so the receipt stayed on the issuing store. POST /act-export on the issuing host returned the three public artifacts. POST /operator-pin act-accept returned 200 accepted. The walk did not hardcode public /act-accept as the accept URL. Public GET /instances stayed 403. Public POST /act-export stayed 403. After Decommission and POST /kill-export, POST /operator-pin kill-accept returned 200. The same POST /runtime-check refused because this store accepted a kill. A following POST /check-wimse of that historical present on the public host also refused because this store accepted a kill. Live public POST /operator-pin and POST /well-known-follow stayed 403. The public host wrote no instance record. See see-walk/later-ui-public-wimse-act-accept/SEE.txt.

The laboratory test count at this close is 501 tests. This page does not say the bet is won.

## Rung 84 — laboratory operator page well-known operator-pin follow

23 August 2026. GET /laboratory verifier pin posts follow operator_pin_paths, checks[], and verifier-challenge from the typed or configured check base well-known document. Same-origin follows this host document. Off-origin uses POST /well-known-follow and POST /operator-pin. The browser does not fetch the public name. www, the apex, HTTP to the public name, and a public port other than 443 stay refused. A missing pin is refused. A write-verb pin is refused. Issuing-store write paths stay hardcoded on this store. Birth, Present, and Death headings stay parked. GET /status is JSON and does not post verifier pins. The issuing-store host does not spawn AgentProcess. The published public well-known document did not change. Public host not restarted.

The laboratory test count at this close is 502 tests. This page does not say the bet is won.

## Rung 85 — laboratory operator page typed check base

23 August 2026. GET /laboratory Check types http://127.0.0.1 or https://check.prestigeworldwide.digital. This origin follows GET /.well-known/prometheus-check and posts the documented check path. A different accepted base uses POST /runtime-check. The browser does not fetch the public name. Off-name bases and HTTP to the public name stay refused. fetch("/check-svid") and fetch("/check-wimse") are not the only check path. POST /runtime-check already allowed then refused against Store B. The issuing-store host does not spawn AgentProcess. Birth, Present, and Death headings stay parked. The published public well-known document did not change. Public host not restarted.

The laboratory test count at this close is 503 tests. This page does not say the bet is won.

## Rung 86 — laboratory operator page Check again

23 August 2026. GET /laboratory Check again was missing. The page now posts the same present to the typed check base without retyping. Each click hits the host. ALLOWED is not stored in the browser. Issuing GET /laboratory on 127.0.0.1:18818. POST /well-known-follow resolved issuer-accept and kill-accept. POST /operator-pin issuer-accept 200. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest laboratory X.509-SVID present. Check again of that same present also allowed. After Decommission and POST /kill-export, POST /operator-pin kill-accept returned 200. The walk did not hardcode public /kill-accept as the accept URL. The same POST /runtime-check refused because this store accepted a kill, not expiry. A following POST /check-svid of that historical present on the public host also refused because this store accepted a kill. Live POST /runtime-check, POST /operator-pin, and POST /well-known-follow on the public name stayed 403. The issuing-store host does not spawn AgentProcess. Birth, Present, and Death headings stay parked. GET /laboratory does not claim Check both or a named act. See see-walk/later-ui-laboratory-public-check/SEE.txt.

The laboratory test count at this close is 504 tests. This page does not say the bet is won.


## Rung 87 — laboratory operator page WIMSE Check again against the live public name

23 August 2026. GET /laboratory Check again already posts the last WIMSE present. Issuing GET /laboratory on 127.0.0.1:18820. POST /well-known-follow resolved issuer-accept and kill-accept. POST /operator-pin issuer-accept 200. POST /present-wimse minted a laboratory Workload Identity Token plus Content-Digest. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed. Check again of that same WIMSE present also allowed. After Decommission and POST /kill-export, POST /operator-pin kill-accept returned 200. The walk did not hardcode public /kill-accept as the accept URL. The same POST /runtime-check refused because this store accepted a kill, not expiry. A following POST /check-wimse of that historical present on the public host also refused because this store accepted a kill. Live POST /runtime-check, POST /operator-pin, and POST /well-known-follow on the public name stayed 403. The issuing-store host does not spawn AgentProcess. Birth, Present, and Death headings stay parked. GET /laboratory does not claim Check both or a named act. Rust was not added. See see-walk/later-ui-laboratory-public-wimse/SEE.txt.

The laboratory test count at this close is 504 tests. This page does not say the bet is won.

A later list of what a person can prove without the command line lives in JUDGE-NON-CLI.md.

## Rung 88 — later user interface Check again against the live public name

23 August 2026. GET / on 127.0.0.1:18822 is the later user interface. Check again already posts the last present. POST /well-known-follow resolved issuer-accept and kill-accept. POST /operator-pin issuer-accept 200. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed an honest laboratory X.509-SVID present. Check again of that same present also allowed. After Decommission and POST /kill-export, POST /operator-pin kill-accept returned 200. The walk did not hardcode public /kill-accept as the accept URL. The same POST /runtime-check refused because this store accepted a kill, not expiry. A following POST /check-svid of that historical present on the public host also refused because this store accepted a kill. Live POST /runtime-check, POST /operator-pin, and POST /well-known-follow on the public name stayed 403. The issuing-store host does not spawn AgentProcess. Rust was not added. See see-walk/later-ui-public-check-again/SEE.txt.

The laboratory test count at this close is 504 tests. This page does not say the bet is won.

## Rung 89 — later user interface WIMSE Check again against the live public name

23 August 2026. GET / on 127.0.0.1:18824 is the later user interface. Check again already posts the last WIMSE present. POST /well-known-follow resolved issuer-accept and kill-accept. POST /operator-pin issuer-accept 200. POST /present-wimse minted a laboratory Workload Identity Token plus Content-Digest. POST /runtime-check with check_base https://check.prestigeworldwide.digital allowed. Check again of that same WIMSE present also allowed. After Decommission and POST /kill-export, POST /operator-pin kill-accept returned 200. The walk did not hardcode public /kill-accept as the accept URL. The same POST /runtime-check refused because this store accepted a kill, not expiry. A following POST /check-wimse of that historical present on the public host also refused because this store accepted a kill. Live POST /runtime-check, POST /operator-pin, and POST /well-known-follow on the public name stayed 403. The issuing-store host does not spawn AgentProcess. Rust was not added. See see-walk/later-ui-public-wimse-check-again/SEE.txt.

The laboratory test count at this close is 504 tests. This page does not say the bet is won.


## Rung 90 — later user interface Check both against the live public name

23 August 2026. GET / on 127.0.0.1:18826 is the later user interface. Check both already posts POST /runtime-check once per present. POST /well-known-follow resolved issuer-accept and kill-accept. POST /operator-pin issuer-accept 200. Create Agent Principal wrote a parent. POST /spawn wrote a narrower child. Two POST /runtime-check hits with check_base https://check.prestigeworldwide.digital allowed. After parent Decommission and POST /kill-export, POST /operator-pin kill-accept returned 200. The walk did not hardcode public /kill-accept as the accept URL. The same two POST /runtime-check hits refused because this store accepted a kill cascade, not expiry. A named check of the child also refused because this store accepted a kill cascade. A following POST /check-svid of that historical child present on the public host also refused because this store accepted a kill. Live POST /runtime-check, POST /operator-pin, and POST /well-known-follow on the public name stayed 403. The issuing-store host does not spawn AgentProcess. Rust was not added. See see-walk/later-ui-public-check-both/SEE.txt.

The laboratory test count at this close is 504 tests. This page does not say the bet is won.

## Rung 91 — later user interface named check of an independent live act against the live public name

23 August 2026. GET / on 127.0.0.1:18828 is the later user interface. Two independent Create Agent Principal writes. POST /well-known-follow resolved issuer-accept and kill-accept. POST /operator-pin issuer-accept 200. Two POST /runtime-check hits with check_base https://check.prestigeworldwide.digital allowed. After Decommission of the first instance and POST /kill-export, POST /operator-pin kill-accept returned 200. The accepted kill list includes the first instance and does not include the second instance. Check both is not ALLOWED. A named check of the live second act allowed. A named check of the first act refused because this store accepted a kill, not expiry and not a cascade. Live POST /runtime-check, POST /operator-pin, and POST /well-known-follow on the public name stayed 403. The issuing-store host does not spawn AgentProcess. Rust was not added. The public Check-both matrix stops here. See see-walk/later-ui-public-named-act/SEE.txt.

The laboratory test count at this close is 504 tests. This page does not say the bet is won.

## Rung 92 — challenge after remaining life refused

23 August 2026. Kernel issue_holder_challenge and POST /challenge append a signed issuance.log line. After remaining life that write is refused. A refused challenge does not append a new challenge line. Tests: issue_holder_challenge_after_issuer_seal_is_refused and the_host_challenge_path_refuses_after_issuer_seal. POST /sign-holder-nonce after remaining life was already refused by sign_holder_nonce_after_issuer_seal_is_refused. POST /verifier-challenge after remaining life stays allowed and writes no issuance.log line. GET /instances after remaining life stays allowed. SOURCE-OF-TRUTH.md and ANATOMY.md now name challenge in the after-seal refuse list.

The laboratory test count at this close is 506 tests. This page does not say the bet is won.

## Rung 93 — member-two and threshold after issuer seal refused

23 August 2026. Kernel add_issuer_member, set_issuer_threshold, and set_verify_threshold write issuer.json and append a signed issuance.log line. After the seal write, during remaining life, those writes were allowed on the kernel. The host already refused POST /member-two, POST /set-issuer-threshold, and POST /set-verify-threshold when kill_date was set. The command line can skip the host. The kernel now refuses the same way as rotate. Tests: add_issuer_member_after_issuer_seal_is_refused, set_issuer_threshold_after_issuer_seal_is_refused, set_verify_threshold_after_issuer_seal_is_refused, and the_host_set_verify_threshold_refuses_after_issuer_seal. Host tests the_host_member_two_path_refuses_after_issuer_seal and the_host_set_issuer_threshold_refuses_after_issuer_seal were already present. SOURCE-OF-TRUTH.md already named member-two and both thresholds in the after-seal refuse list.

The laboratory test count at this close is 510 tests. This page does not say the bet is won.

## Rung 94 — intended export after remaining life

23 August 2026. sign_issuance_log_tree_head refused after remaining life. Seal-export and kill-export call that sign. The existing test the_host_seal_export_path_returns_event_proof_and_tree_head_after_seal only covered remaining life still open. After remaining life that export was refused. Kill after remaining life was already allowed by the_host_kill_path_still_revokes_after_host_seal. The kernel now signs a tree head after remaining life. Tests: seal_export_after_remaining_life_returns_artifacts_a_verifier_can_accept, kill_export_after_remaining_life_returns_artifacts_a_verifier_can_accept, the_host_seal_export_path_returns_event_proof_and_tree_head_after_remaining_life, and the_host_kill_export_path_returns_event_proof_and_tree_head_after_remaining_life. Mint, birth, spawn, present, check, challenge, rotate, member-two, and both thresholds stay refused. Command-line leftover writes that skip the host were already gated on the kernel after Rung 93.

The laboratory test count at this close is 514 tests. This page does not say the bet is won.

## Rung 95 — remaining verifier artifacts after remaining life

23 August 2026. After remaining life, act-export of a receipt from a check that happened before remaining life still returns receipt, proof, and tree_head. Store B can act-accept that bundle. Previous-key-export still returns the previous public key and kill date. GET /issuer-public still returns the current issuer public key. Those paths were already allowed. This rung locks the named allow. Tests: act_export_after_remaining_life_returns_artifacts_a_verifier_can_accept, previous_key_after_remaining_life_still_pins_on_store_b, issuer_public_listing_after_remaining_life_still_returns_the_current_key, the_host_act_export_path_returns_receipt_proof_and_tree_head_after_remaining_life, the_host_previous_key_export_path_returns_public_artifacts_after_remaining_life, and the_host_issuer_public_path_returns_the_current_key_after_remaining_life. Mint, birth, spawn, present, check, challenge, rotate, member-two, and both thresholds stay refused. A sealed issuing store still cannot pin a foreign issuer public key.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 96 — member two remote path over site-to-site

23 August 2026. A throwaway issuing store on hostname 5090 used issuer member add --secret-path through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. The kernel wrote member two onto the isolated VPC disk. The standing laptop member-two.secret was not copied. issuer.secret stayed on the issuing computer. After fusermount, birth refused (threshold_n 2, only 1 issuer member secret). After remount, birth with --member-secret on that path succeeded with two member signatures. Public check was not done. No throwaway pin was left on the public check host. Rust was not changed.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 97 — member two remote path public allow then refuse

23 August 2026. A throwaway issuing store on hostname 5090 used issuer member add --secret-path through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. The kernel wrote member two onto the isolated VPC disk. The standing laptop member-two.secret was not copied. issuer.secret stayed on the issuing computer. After issuance threshold_n 2, Create Agent Principal and present --format x509-svid used --member-secret on that remote path. POST /issuer-accept on https://check.prestigeworldwide.digital pinned this throwaway public key only. Holder-sign ran locally. POST /check-svid allowed. After Decommission and kill export, POST /kill-accept returned 200. The same historical Assertion Act then refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. Rust was not changed. See see-walk/member-two-vpc-public-check/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 98 — later user interface remote member-secret path

23 August 2026. GET / on 127.0.0.1:18832 is the later user interface. POST /member-two wrote member two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /birth without member_secret_path returned 403. After fusermount, POST /birth with that remote path returned 403 because threshold_n is 2 but only 1 issuer member secret is present. After remount, POST /birth returned 200. POST /present-svid with the same remote path returned 200 and two issuer member signatures. The standing issuer was not used. issuer.secret stayed on the issuing computer. VPC issuer.secret count stayed 0. Rust was not changed. See see-walk/later-ui-member-two-remote/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 99 — later user interface remote member-secret path public allow then refuse

23 August 2026. GET / on 127.0.0.1:18834 is the later user interface. POST /member-two wrote member two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /agent-type used member_secret_path after that raise. POST /birth without member_secret_path returned 403. POST /birth and POST /present-svid with the remote path returned 200 and two issuer member signatures. POST /issuer-accept on https://check.prestigeworldwide.digital pinned this throwaway public key only. Holder-sign ran locally. POST /check-svid allowed. After Decommission and POST /kill-export with member_secret_path, POST /kill-accept returned 200. The same historical Assertion Act then refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not used. issuer.secret stayed on the issuing computer. VPC issuer.secret count stayed 0. Rust was not changed. See see-walk/later-ui-member-two-public/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 100 — later user interface remote member-secret path WIMSE public allow then refuse

23 August 2026. GET / on 127.0.0.1:18836 is the later user interface. POST /member-two wrote member two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /agent-type used member_secret_path after that raise. POST /birth without member_secret_path returned 403. POST /birth and POST /present-wimse with the remote path returned 200 and two issuer member signatures. The Workload Identity Token subject is the present-hash wimse Uniform Resource Identifier. POST /issuer-accept on https://check.prestigeworldwide.digital pinned this throwaway public key only. Holder-sign ran locally. POST /check-wimse allowed. After Decommission and POST /kill-export with member_secret_path, POST /kill-accept returned 200. The same historical Assertion Act then refused because this store accepted a kill, not expiry. The throwaway store was shredded. The VPC custody directory was left in place. The standing issuer was not used. issuer.secret stayed on the issuing computer. VPC issuer.secret count stayed 0. Rust was not changed. See see-walk/later-ui-member-two-public-wimse/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 101 — laboratory operator page remote member-secret path

23 August 2026. GET /laboratory on 127.0.0.1:18838 is the laboratory operator page. GET / on the same host is the later user interface and is a different page. POST /member-two wrote member two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /birth without member_secret_path returned 403. After fusermount, POST /birth with that remote path returned 403 because threshold_n is 2 but only 1 issuer member secret is present. After remount, POST /birth returned 200. POST /present-svid with the same remote path returned 200 and two issuer member signatures. The standing issuer was not used. issuer.secret stayed on the issuing computer. VPC issuer.secret count stayed 0. Public check of this present was not done. Rust was not changed. See see-walk/later-ui-laboratory-member-two-remote/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 102 — laboratory operator page remote member-secret path public allow then refuse

23 August 2026. GET /laboratory on 127.0.0.1:18842 is the laboratory operator page. GET / on the same host is the later user interface and is a different page. POST /member-two wrote member two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /birth without member_secret_path returned 403. POST /birth with the remote path returned 200. POST /present-svid with the same remote path returned 200 and two issuer member signatures. POST /issuer-accept on https://check.prestigeworldwide.digital pinned only this throwaway public key. Holder-sign ran locally. POST /check-svid allowed. After Decommission and POST /kill-export, POST /kill-accept returned 200. The same historical Assertion Act then refused because this store accepted a kill, not expiry. The standing issuer was not used. issuer.secret stayed on the issuing computer. issuer.secret was not on the mount. Rust was not changed. See see-walk/later-ui-laboratory-member-two-public/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 103 — laboratory operator page remote member-secret path WIMSE public allow then refuse

23 August 2026. GET /laboratory on 127.0.0.1:18844 is the laboratory operator page. GET / on the same host is the later user interface and is a different page. POST /member-two wrote member two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /agent-type used member_secret_path after that raise. POST /birth without member_secret_path returned 403. POST /birth and POST /present-wimse with the remote path returned 200 and two issuer member signatures. The Workload Identity Token subject is the present-hash wimse Uniform Resource Identifier. POST /issuer-accept on https://check.prestigeworldwide.digital pinned only this throwaway public key. Holder-sign ran locally. POST /check-wimse allowed. After Decommission and POST /kill-export, POST /kill-accept returned 200. The same historical Assertion Act then refused because this store accepted a kill, not expiry. The standing issuer was not used. issuer.secret stayed on the issuing computer. issuer.secret was not on the mount. Direct SSH issuer.secret count stayed 0. Rust was not changed. See see-walk/later-ui-laboratory-member-two-public-wimse/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 104 — agent-process on hostname 5090 with remote member-two public allow then refuse

23 August 2026. A throwaway issuing store on hostname 5090 used POST /member-two through an SSHFS mount of ubuntu@10.43.1.186:/home/ubuntu/member-two-custody. POST /set-issuer-threshold set threshold_n to 2 on this throwaway issuer only. POST /birth without member_secret_path returned 403. POST /birth and POST /present-svid with the remote path returned 200 and two issuer member signatures. POST /issuer-accept on https://check.prestigeworldwide.digital pinned this throwaway public key only. LaboratoryRuntime still refused --holder-secret-path. `prometheus runtime-check agent-process` stayed up on hostname 5090 as process identifier 3559917. The first tool line printed ALLOWED and ran the tool. After Decommission, POST /kill-export, and public POST /kill-accept, a second tool line on that same process identifier printed REFUSED because this store accepted a kill, not expiry. The tool did not run. The process did not restart. Hermes was not started. Rust was not changed. See see-walk/member-two-agent-process/SEE.txt.

The laboratory test count at this close is 520 tests. This page does not say the bet is won.

## Rung 105 — issuer-threshold raise without the outside member secret refused

23 August 2026. Kernel set_issuer_threshold is a signed persist when raising issuance threshold_n. The write saved issuer.json at the new n, then append_log signed at that new n. When member two lived only at an outside path and a later process did not present that path, append_log refused after issuer.json already held n=2. The command line can skip the host. The kernel now checks member secrets for the new n before any write. Tests: set_issuer_threshold_needs_the_outside_member_secret and the_host_set_issuer_threshold_raise_refuses_without_the_outside_member_secret. A refused raise leaves threshold_n at 1 and does not append a new issuance.log line. Same-n after n=2 stays a no-op. SOURCE-OF-TRUTH.md and ANATOMY.md now name that raise as a signed persist at the new n.

The laboratory test count at this close is 522 tests. This page does not say the bet is won.

